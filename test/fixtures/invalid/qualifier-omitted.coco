coco QualifierOmitted

class Person:
    field name String
    field age U64

state logic:
    name String

state actor:
    tag String

// EXPECT: endpoint 'GetName' is declared as 'pure', but it requires state qualifier 'static'
endpoint GetName() -> (name String):
    observe name <- QualifierOmitted.Logic.name
